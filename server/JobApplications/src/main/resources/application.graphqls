type Query {
	applications: [Application]
}

type Application {
	accountId: ID!
	date: LocalDateTime
	company: String
	position: String
	currentStatus: String
}

type Mutation {
	newApplication(company: String!, position: String!) : Application!
	deleteApplication(accountID: ID!) : Boolean
	updateApplication(accountID:ID!, currentStatus) : Application!
}